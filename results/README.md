# Results Directory

Generated outputs are written here when the notebooks are run locally.

This directory is ignored by Git because registration outputs, embeddings, WNN graphs, patch-review PDFs, and label-transfer tables are large and dataset-specific. Keep only lightweight documentation in version control.
